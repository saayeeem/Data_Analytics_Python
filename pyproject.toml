[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "data_analytics_python"
version = "0.1.0"
description = "Data analytics, machine learning, NLP, and visualization in Python"
authors = [
    {name = "saayeeem"}
]
readme = "README.md"
requires-python = ">=3.8"
# Uncomment and edit the following line if you have Python dependencies
# dependencies = ["numpy", "pandas", "matplotlib", "scikit-learn"]